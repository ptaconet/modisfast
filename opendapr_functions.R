## Functions of opendapr :

## Les fonctions de opendapr sont ciblées pour certains serveurs opendap : elles n'ont pas pour objectif d'être génériques,
## dans l'article, dire qu'il pourrait être intéressant de construire une interface opendap R générique (ie fonctionne pour tous les serveurs opendap) R interface to OPeNDAP


#getUrls(collection,variables,roi,timeRange,outputFormat="nc4",singleNetcdf=TRUE,optionalsOpendap=NULL,loginCredentials=NULL)

#OK -> getVariablesInfo<-function(collection,loginCredentials=NULL){  # for a given collection, get the available variables and associated information

#OK -> getCollectionInfo<-function(collection){  # for a given collection, get the available variables and associated information

#.getDimensionValues(collection,dimension,loginCredentials=NULL)  # get the values of a dimensions (lat,lon,time) as a vector

#.createURLVariable

#.createURLVariable2

#.getTimeIndex_modis

#.getOptionals

#OK -> downloadData






